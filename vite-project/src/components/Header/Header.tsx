import React from "react";
import { useState } from "react";

const Header: React.FC = () => {
	return (
		<header className="header">
			<h1>Mon Site en SPA</h1>
		</header>
	);
};

export default Header;
